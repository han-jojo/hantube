extends ../base
include ../mixins/video


block content
    div.profile__data
        if (!user.avatarUrl || user.avatarUrl == null)
            img(src="/public/images/no-profile.jpg", alt="no-image").avatar
        else
            img(src=user.avatarUrl, alt=user.avatarUrl).avatar
        h3=user.name
    div.video-grid
        each video in user.videos
            +video(video)
        else
            span.empty__message 업로드된 영상이 없습니다.